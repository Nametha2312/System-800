import { Card, Loading } from '@/components/ui';import { useHealth, useMetrics } from '@/hooks';import type { HealthStatus } from '@/types';const formatUptime = (seconds: number): string => {  const days = Math.floor(seconds / 86400);  const hours = Math.floor((seconds % 86400) / 3600);  const minutes = Math.floor((seconds % 3600) / 60);  if (days > 0) return `${days}d ${hours}h ${minutes}m`;  if (hours > 0) return `${hours}h ${minutes}m`;  return `${minutes}m`;};const formatBytes = (bytes: number): string => {  if (bytes === 0) return '0 B';  const k = 1024;  const sizes = ['B', 'KB', 'MB', 'GB'];  const i = Math.floor(Math.log(bytes) / Math.log(k));  return `${parseFloat((bytes / Math.pow(k, i)).toFixed(1))} ${sizes[i]}`;};export function SystemPage() {  const { data: health, isLoading: healthLoading, error: healthError } = useHealth();  const { data: metrics, isLoading: metricsLoading, error: metricsError } = useMetrics();  if (healthLoading || metricsLoading) {    return <Loading size="lg" />;  }  const typedHealth = health as HealthStatus | undefined;  return (    <div className="space-y-6">      <div>        <h1 className="text-2xl font-bold text-white">System Status</h1>        <p className="text-gray-400 mt-1">Health checks and performance metrics</p>      </div>      {/* Overall Status */}      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">        <Card className="col-span-1">          <div className="flex items-center justify-between">            <div>              <p className="text-sm text-gray-400">System Status</p>              <p className="text-2xl font-bold text-white mt-1">                {typedHealth?.status === 'healthy' ? 'Healthy' :                 typedHealth?.status === 'degraded' ? 'Degraded' : 'Unhealthy'}              </p>            </div>            <div              className={`w-4 h-4 rounded-full ${                typedHealth?.status === 'healthy'                  ? 'bg-green-500'                  : typedHealth?.status === 'degraded'                  ? 'bg-yellow-500'                  : 'bg-red-500'              }`}            />          </div>        </Card>        <Card>          <p className="text-sm text-gray-400">Uptime</p>          <p className="text-2xl font-bold text-white mt-1">            {typedHealth?.uptime ? formatUptime(typedHealth.uptime) : '-'}          </p>        </Card>        <Card>          <p className="text-sm text-gray-400">Timestamp</p>          <p className="text-xl font-bold text-white mt-1">            {typedHealth?.timestamp              ? new Date(typedHealth.timestamp).toLocaleTimeString()              : '-'}          </p>        </Card>      </div>      {/* Service Health */}      <Card>        <h3 className="text-lg font-semibold text-white mb-4">Service Health</h3>        {healthError ? (          <p className="text-red-400">Failed to load health data</p>        ) : (          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">            {typedHealth?.checks?.map((check) => (              <div                key={check.name}                className="p-4 bg-gray-750 rounded-lg flex items-center justify-between"              >                <div>                  <p className="text-white font-medium capitalize">                    {check.name.replace(/_/g, ' ')}                  </p>                  {check.message && (                    <p className="text-sm text-gray-400 mt-1">{check.message}</p>                  )}                </div>                <div                  className={`w-3 h-3 rounded-full flex-shrink-0 ${                    check.healthy ? 'bg-green-500' : 'bg-red-500'                  }`}                />              </div>            ))}          </div>        )}      </Card>      {/* Metrics */}      <Card>        <h3 className="text-lg font-semibold text-white mb-4">Performance Metrics</h3>        {metricsError ? (          <p className="text-red-400">Failed to load metrics</p>        ) : metrics ? (          <div className="space-y-6">            {/* Memory */}            {(metrics as { memory?: { rss?: number; heapUsed?: number; heapTotal?: number; external?: number } }).memory && (() => {              const mem = (metrics as { memory: { rss?: number; heapUsed?: number; heapTotal?: number; external?: number } }).memory;              return (                <div>                  <h4 className="text-sm font-medium text-gray-400 mb-3">Memory Usage</h4>                  <div className="grid grid-cols-2 md:grid-cols-4 gap-4">                    <div className="p-3 bg-gray-750 rounded-lg">                      <p className="text-xs text-gray-400">RSS</p>                      <p className="text-lg font-semibold text-white">{formatBytes(mem.rss ?? 0)}</p>                    </div>                    <div className="p-3 bg-gray-750 rounded-lg">                      <p className="text-xs text-gray-400">Heap Used</p>                      <p className="text-lg font-semibold text-white">{formatBytes(mem.heapUsed ?? 0)}</p>                    </div>                    <div className="p-3 bg-gray-750 rounded-lg">                      <p className="text-xs text-gray-400">Heap Total</p>                      <p className="text-lg font-semibold text-white">{formatBytes(mem.heapTotal ?? 0)}</p>                    </div>                    <div className="p-3 bg-gray-750 rounded-lg">                      <p className="text-xs text-gray-400">External</p>                      <p className="text-lg font-semibold text-white">{formatBytes(mem.external ?? 0)}</p>                    </div>                  </div>                </div>              );            })()}          </div>        ) : (          <p className="text-gray-400">No metrics available</p>        )}      </Card>    </div>  );}

